PLIST_INCLUDE=${CMAKE_INSTALL_PREFIX}/include
PLIST_BINARY=${CMAKE_INSTALL_PREFIX}/bin
PLIST_LIBRARY=${CMAKE_INSTALL_PREFIX}/lib

Name: lib${PROJECT_NAME_CC}
Description: A library to handle Apple Property Lists whereas they are binary or XML
Version: ${PROJECT_VERSION_MAJOR}.${PROJECT_VERSION_MINOR}.${PROJECT_VERSION_PATCH}
Libs: -L${CMAKE_INSTALL_PREFIX}/lib -l${PROJECT_NAME_CC}-${PROJECT_VERSION_SUFFIX}
Cflags: -I${CMAKE_INSTALL_PREFIX}/include
